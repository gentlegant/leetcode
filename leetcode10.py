class Solution:
    def isMatch(self, s, p):
        """
        :type s: str
        :type p: str
        :rtype: bool
        """
        re.fullmatch()
        import re
        return True  if re.fullmatch(p,s)!=None else False